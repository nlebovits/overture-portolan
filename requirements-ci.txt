# The validator pins that every CI run in this repository reads.
#
# Three workflows install from this file: ci.yml, sync.yml, and data-pass.yml.
# One file keeps all three on one validator, so a gate cannot mean one thing on
# a pull request and a different thing in a weekly run.
#
# The template took a floor here, not a pin, so a repo made from it picks up
# each new 0.1.x release. This catalog pins instead, because it needs a fix
# that is merged but not yet released.
#
# The pin is the merge commit of portolan-sdi/rashid#167, which moves
# PTL-DAT-006 off a per-row read and onto footer statistics. Measured on one
# remote Overture part, that takes the data pass from 48.5s to 7.1s.
#
# tools/check_rashid_release.py replaces the line below with 'rashid==X.Y.Z'
# when PyPI carries a release above 0.1.7, and
# .github/workflows/rashid-release.yml runs it every week. Do not reformat the
# line by hand: the tool matches one of two exact shapes and it fails when it
# matches neither.
#
# stac-check stays unpinned on purpose. The stac-check exemption in
# docs/conformance.md expires when the upstream crash stops. That only happens
# if CI takes each new release.
rashid==0.1.8
stac-check
